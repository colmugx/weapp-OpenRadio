{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "微信小程序",
            "type": "weapp",
            "request": "launch",
            "project": "${workspaceRoot}"
        }
    ]
}
